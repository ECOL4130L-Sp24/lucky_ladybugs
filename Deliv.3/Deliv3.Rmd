---
title: "deliv.3"
author: "Niko"
date: "`r Sys.Date()`"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
```{r}
#Load the tidyverse package

#Load the here package
 #install.packages ('tidyverse', 'here')
#library("readr")
library("tidyverse")
library("here")
# install.packages("devtools")

#install.packages("rvest")
```
```{r}
raw_biomass<-read_csv("data/raw/alg_biomass.csv")
raw_perFish<-read_csv("data/raw/fsh_perFish.csv")
raw_perPass<-read_csv("data/raw/fsh_perPass.csv")
raw_waq<-read_csv("data/raw/waq_instantaneous.csv")
```
```{r}
joined<-full_join(raw_biomass,raw_perFish)%>%
  full_join(., raw_perPass) %>%
full_join(., raw_waq)


head(joined)
```

